import React from "react";
import IPage from "../interfaces/pages";

const SignOut: React.FunctionComponent<IPage> = (props) => {
  return (
    <div>
      <h1>Sign Out Page</h1>
    </div>
  );
};

export default SignOut;
